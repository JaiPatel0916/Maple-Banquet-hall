const DashboardHome = () => {
    return (
        <div className="text-gray-600 text-sm">
            Select an option above to manage admin data.
        </div>
    );
};

export default DashboardHome;
